using UnityEngine;
using System.Collections;

public class PowerUpCollisionResponse : MonoBehaviour {
	// Use this for initialization
	void Start () {
	
	}
	
	void OnCollisionEnter(Collision CollsionInfo){
		/*TrainCarrages PlayerTrainCarrages;
		
		if (CollsionInfo.gameObject.tag == "Train"){
			PlayerTrainCarrages = CollsionInfo.gameObject.GetComponent<Carrages>();
			PlayerTrainCarrages.AppendCarrage();
		}
		
		this.renderer.enabled = false;*/
	}	
	
	// Update is called once per frame
	void Update () {
	
	}
}
